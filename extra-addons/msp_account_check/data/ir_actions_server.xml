<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="check_reprint_action" model="ir.actions.server">
            <field name="name">Reprint Check</field>
            <field name="model_id" ref="model_account_check"/>
            <field name="binding_model_id" ref="model_account_check"/>
            <field name="state">code</field>
            <field name="code">
for record in records:
    if record.state == 'issued':
        record.write({'state':'in-portfolio'})
            </field>
        </record>

    </data>
</odoo>